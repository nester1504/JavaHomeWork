import java.io.IOException;
import java.util.Arrays;
import java.util.logging.FileHandler;
import java.util.logging.Level;

public class Main {
    public static void main(String[] args) {


        Task1.sQLQueryBuilder();

        // int[] array = {5, 2, 8, 12, 3, 7, 45, 2, 5, 7};

      //  try {
       //     FileHandler fileHandler = new FileHandler("log.txt");
       //     fileHandler.setLevel(Level.INFO);

       //     Task2.logger.addHandler(fileHandler);

        //    Task2.logger.setLevel(Level.INFO);

       //     Task2.logger.info("Исходный массив: " + Arrays.toString(array));

        //    Task2.bubbleSort(array);

        //    Task2.logger.info("Отсортированный массив: " + Arrays.toString(array));
       // } catch (IOException e) {
        //    System.out.println("Ошибка при записи логов в файл: " + e.getMessage());
      //  }


    }
}
